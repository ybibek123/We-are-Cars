First You need to install Xampp for database
Activate MySQL from Xampp
Then you need to create a database inside the database named as "wearecars"
Then import a table (given inside "env" -> inside WeAreCars) "newbookings.sql" inside that database
Then run this code in intellij or any IDE
Then delete (read.me and env) after setup.

For staff login
Username : sta001
Password : givemethekeys123